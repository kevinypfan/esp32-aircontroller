# 臥室冷氣 — 燒入用這個檔
#   esphome run aircon-bedroom.yaml --device maxe-aircon-bedroom.local
#
# 第一次燒入（新的 ESP，還沒上 WiFi）要用 USB：
#   esphome run aircon-bedroom.yaml --device COMx
#   （USB 燒入務必在「還沒接冷氣」的狀態下做！接了冷氣只能走 OTA）

substitutions:
  device_name: maxe-aircon-bedroom
  friendly_name: 臥室冷氣

packages:
  common: !include maxe-aircon-common.yaml
